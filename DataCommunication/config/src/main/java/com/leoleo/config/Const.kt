package com.leoleo.config

object Const {
    object MsgEvent {
        const val PATH = "/data_comm2"
    }
    object DataEvent {
        const val DATA_PATH = "/data_comm"
        const val KEY_DATA = "key_data"
    }
}